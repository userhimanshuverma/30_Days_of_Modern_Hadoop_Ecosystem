<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>
    <!-- Hive Metastore Warehouse Directory on Object Storage (MinIO / S3A) -->
    <property>
        <name>hive.metastore.warehouse.dir</name>
        <value>s3a://warehouse/hive-tables/</value>
        <description>Default location for managed Hive databases and tables on cloud storage.</description>
    </property>

    <!-- JDBC Connection to Metastore Database -->
    <property>
        <name>javax.jdo.option.ConnectionURL</name>
        <value>jdbc:postgresql://metastore-db:5432/metastore_db</value>
    </property>

    <property>
        <name>javax.jdo.option.ConnectionDriverName</name>
        <value>org.postgresql.Driver</value>
    </property>

    <property>
        <name>javax.jdo.option.ConnectionUserName</name>
        <value>hive</value>
    </property>

    <property>
        <name>javax.jdo.option.ConnectionPassword</name>
        <value>hivepassword</value>
    </property>

    <!-- Enable Hive External Table Defaulting -->
    <property>
        <name>hive.metastore.disallow.incompatible.col.type.changes</name>
        <value>false</value>
    </property>

    <!-- Metastore Execution URIs -->
    <property>
        <name>hive.metastore.uris</name>
        <value>thrift://hive-metastore:9083</value>
    </property>

    <!-- Enforce Explicit Location for Cloud Tables -->
    <property>
        <name>hive.metastore.schema.verification</name>
        <value>false</value>
    </property>

    <!-- Disable File Listing Cache for Direct Cloud Storage Metadata Queries -->
    <property>
        <name>hive.stats.autogather</name>
        <value>true</value>
    </property>
</configuration>
